{
  "name": "photoai-monorepo",
  "private": true,
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run dev --prefix backend",
    "dev": "npx -y concurrently \"npm run dev:frontend\" \"npm run dev:backend\""
  }
}
